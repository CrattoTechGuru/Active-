# URL of the backend API (used at build time for direct calls in production)
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# Used by next.config.js rewrites in local dev
BACKEND_URL=http://localhost:4000
